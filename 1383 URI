#include<stdio.h>
int main()
{
  int sud[9][9], i, j, total=0, n, instancia=1, cont=0, sim=1;
  int c=0, c1=3;
  scanf("%d", &n);
 for(; n>0; n--)
 {
   for(i=0; i<9; i++)
     for(j=0; j<9; j++)
      scanf("%d", &sud[i][j]);
        for(; cont<3; cont++)       //contagem da linha 0 ate 2
        {
         for(i=0; i<3; i++)
         {
          for(j=c; j<c1; j++)
          {
            total = total + sud[i][j];
            if(sud[i][j]==0)
             {
              sim=0;
               break;
                    }

                }

            }
            if(total != 45)
            {               
                sim=0;
                break;
            }

            c=c+3;
            c1=c1+3;
            total=0;
        }
        cont=0;
        total=0;
        c=0;
        c1=3;
        for(; cont<3; cont++)     //contagem da linha 3 ate 5
        {
            for(i=3; i<6; i++)
            {
                for(j=c; j<c1; j++)
                {
                    total = total + sud[i][j];
                    if(sud[i][j] == 0)
                    {
                        sim=0;
                        break;
                    }

                }

            }
            if(total != 45)
            {               
                sim=0;
                break;
            }

            c=c+3;
            c1=c1+3;
            total=0;
        }
        cont=0;
        total=0;
        c=0;
        c1=3;
        for(; cont<3; cont++)    //contagem da linha 6 ate 8
        {
            for(i=6; i<9; i++)
            {
                for(j=c; j<c1; j++)
                {
                    total = total + sud[i][j];
                    if(sud[i][j] == 0)
                    {
                        sim=0;
                        break;
                    }

                }

            }
            if(total != 45)
            {               
                sim=0;
                break;
            }

            c=c+3;
            c1=c1+3;
            total=0;
        }

        for(i=0; i<9; i++)  //contagem das linhas
        {
            for(j=0; j<9; j++)
            {   
                total=total+sud[i][j];
            }
            if(total!=45)
                {
                    sim=0;
                    break;
                }
            total=0;
        }

        for(j=0; j<9; j++)   //contagem das colunas
        {
            for(i=0; i<9; i++)
            {
                total=total+sud[i][j];    
            }
            if(total!=45)
                {
                    sim=0;
                    break;
                }
            total=0;
        }

    if(sim)
        printf("Instancia %d\nSIM\n", instancia);
    else
        printf("Instancia %d\nNAO\n", instancia);
    printf("\n");

    c=0;
    c1=3;
    instancia++;
    sim=1;
    total=0;

}
return 0;
}
